<%- include("partials/header"); -%>

    <h1>Home</h1>
    <p> <%= homeText %></p>

    <!-- forEach Method Kullanimi -->


   <% blogPosts.forEach(function(element){ %>
        <h1><%= element.title %></h1>
        <p><%=  element.post.substring(0, 3) + "..." %>
            <a href="/posts/<%=element.title%>">Read More</a>
        </p>
    <% }) %>


    <!-- <% for(let i=0; i<blogPosts.length; i++){ %>
        <h1><%= blogPosts[i].title %></h1>
        <p><%=  blogPosts[i].post %></p>
    <% } %> -->


<%- include("partials/footer"); -%>